From: Marek Olšák <maraeo@gmail.com>
Subject: drm/radeon/kms: check AA resolve registers on r300
References: bnc#674693
Git-commit: fff1ce4dc6113b6fdc4e3a815ca5fd229408f8ef
Patch-mainline: v2.6.38-rc5


Signed-off-by: Egbert Eich <eich@suse.de>

This is an important security fix because we allowed arbitrary values
to be passed to AARESOLVE_OFFSET. This also puts the right buffer address
in the register. Also included fix from Git-commit: 45039c.

Signed-off-by: Marek Olšák <maraeo@gmail.com>
Signed-off-by: Dave Airlie <airlied@redhat.com>
---
 drivers/gpu/drm/radeon/r100.c         |   23 +++++++++++++++++++++++
 drivers/gpu/drm/radeon/r100_track.h   |    4 +++-
 drivers/gpu/drm/radeon/r300.c         |   21 +++++++++++++++++++++
 drivers/gpu/drm/radeon/r300_reg.h     |    2 ++
 drivers/gpu/drm/radeon/reg_srcs/r300  |    3 ---
 drivers/gpu/drm/radeon/reg_srcs/rs600 |    3 ---
 drivers/gpu/drm/radeon/reg_srcs/rv515 |    3 ---
 8 files changed, 49 insertions(+), 13 deletions(-)

Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r100.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/r100.c
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r100.c
@@ -2727,6 +2727,26 @@ int r100_cs_track_check(struct radeon_de
 			return -EINVAL;
 		}
 	}
+	if (track->aa_dirty && track->aaresolve) {
+		if (track->aa.robj == NULL) {
+			DRM_ERROR("[drm] No buffer for AA resolve buffer %d !\n", i);
+			return -EINVAL;
+		}
+		/* I believe the format comes from colorbuffer0. */
+		size = track->aa.pitch * track->cb[0].cpp * track->maxy;
+		size += track->aa.offset;
+		if (size > radeon_object_size(track->aa.robj)) {
+			DRM_ERROR("[drm] Buffer too small for AA resolve buffer %d "
+				  "(need %lu have %lu) !\n", i, size,
+				  radeon_object_size(track->aa.robj));
+			DRM_ERROR("[drm] AA resolve buffer %d (%u %u %u %u)\n",
+				  i, track->aa.pitch, track->cb[0].cpp,
+				  track->aa.offset, track->maxy);
+			return -EINVAL;
+		}
+	}
+	track->aa_dirty = false;
+
 	prim_walk = (track->vap_vf_cntl >> 4) & 0x3;
 	nverts = (track->vap_vf_cntl >> 16) & 0xFFFF;
 	switch (prim_walk) {
@@ -2786,6 +2806,8 @@ void r100_cs_track_clear(struct radeon_d
 {
 	unsigned i, face;
 
+	track->aa_dirty = true;
+
 	if (rdev->family < CHIP_R300) {
 		track->num_cb = 1;
 		if (rdev->family <= CHIP_RS200)
@@ -2799,6 +2821,8 @@ void r100_cs_track_clear(struct radeon_d
 		track->num_texture = 16;
 		track->maxy = 4096;
 		track->separate_cube = 0;
+		track->aaresolve = false;
+		track->aa.robj = NULL;
 	}
 
 	for (i = 0; i < track->num_cb; i++) {
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r100_track.h
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/r100_track.h
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r100_track.h
@@ -65,9 +65,12 @@ struct r100_cs_track {
 	struct r100_cs_track_array	arrays[11];
 	struct r100_cs_track_cb 	cb[R300_MAX_CB];
 	struct r100_cs_track_cb 	zb;
+	struct r100_cs_track_cb 	aa;
 	struct r100_cs_track_texture	textures[R300_TRACK_MAX_TEXTURE];
 	bool				z_enabled;
 	bool                            separate_cube;
+	bool				aa_dirty;
+	bool				aaresolve;
 
 };
 
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r300.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/r300.c
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r300.c
@@ -685,6 +685,27 @@ static int r300_packet0_check(struct rad
 		ib[idx] = idx_value + ((u32)reloc->lobj.gpu_offset);
 		track->textures[i].robj = reloc->robj;
 		break;
+	case R300_RB3D_AARESOLVE_OFFSET:
+		r = r100_cs_packet_next_reloc(p, &reloc);
+		if (r) {
+			DRM_ERROR("No reloc for ib[%d]=0x%04X\n",
+				  idx, reg);
+			r100_cs_dump_packet(p, pkt);
+			return r;
+		}
+		track->aa.robj = reloc->robj;
+		track->aa.offset = idx_value;
+		track->aa_dirty = true;
+		ib[idx] = idx_value + ((u32)reloc->lobj.gpu_offset);
+		break;
+	case R300_RB3D_AARESOLVE_PITCH:
+		track->aa.pitch = idx_value & 0x3FFE;
+		track->aa_dirty = true;
+		break;
+	case R300_RB3D_AARESOLVE_CTL:
+		track->aaresolve = idx_value & 0x1;
+		track->aa_dirty = true;
+		break;
 	/* Tracked registers */
 	case 0x2084:
 		/* VAP_VF_CNTL */
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r300_reg.h
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/r300_reg.h
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/r300_reg.h
@@ -1368,6 +1368,8 @@
 #define R300_RB3D_COLORPITCH2               0x4E40 /* GUESS */
 #define R300_RB3D_COLORPITCH3               0x4E44 /* GUESS */
 
+#define R300_RB3D_AARESOLVE_OFFSET          0x4E80
+#define R300_RB3D_AARESOLVE_PITCH           0x4E84
 #define R300_RB3D_AARESOLVE_CTL             0x4E88
 /* gap */
 
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/r300
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/reg_srcs/r300
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/r300
@@ -705,9 +705,6 @@ r300 0x4f60
 0x4E74 RB3D_CMASK_WRINDEX
 0x4E78 RB3D_CMASK_DWORD
 0x4E7C RB3D_CMASK_RDINDEX
-0x4E80 RB3D_AARESOLVE_OFFSET
-0x4E84 RB3D_AARESOLVE_PITCH
-0x4E88 RB3D_AARESOLVE_CTL
 0x4EA0 RB3D_DISCARD_SRC_PIXEL_LTE_THRESHOLD
 0x4EA4 RB3D_DISCARD_SRC_PIXEL_GTE_THRESHOLD
 0x4F04 ZB_ZSTENCILCNTL
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/rs600
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/reg_srcs/rs600
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/rs600
@@ -705,9 +705,6 @@ rs600 0x6d40
 0x4E74 RB3D_CMASK_WRINDEX
 0x4E78 RB3D_CMASK_DWORD
 0x4E7C RB3D_CMASK_RDINDEX
-0x4E80 RB3D_AARESOLVE_OFFSET
-0x4E84 RB3D_AARESOLVE_PITCH
-0x4E88 RB3D_AARESOLVE_CTL
 0x4EA0 RB3D_DISCARD_SRC_PIXEL_LTE_THRESHOLD
 0x4EA4 RB3D_DISCARD_SRC_PIXEL_GTE_THRESHOLD
 0x4F04 ZB_ZSTENCILCNTL
Index: linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/rv515
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/gpu/drm/radeon/reg_srcs/rv515
+++ linux-2.6.32-SLE11-SP1/drivers/gpu/drm/radeon/reg_srcs/rv515
@@ -459,9 +459,6 @@ rv515 0x6d40
 0x4E74 RB3D_CMASK_WRINDEX
 0x4E78 RB3D_CMASK_DWORD
 0x4E7C RB3D_CMASK_RDINDEX
-0x4E80 RB3D_AARESOLVE_OFFSET
-0x4E84 RB3D_AARESOLVE_PITCH
-0x4E88 RB3D_AARESOLVE_CTL
 0x4EA0 RB3D_DISCARD_SRC_PIXEL_LTE_THRESHOLD
 0x4EA4 RB3D_DISCARD_SRC_PIXEL_GTE_THRESHOLD
 0x4EF8 RB3D_CONSTANT_COLOR_AR
