From: jbeulich@novell.com
Subject: Module use count must be updated as bridges are created/destroyed
Patch-mainline: unknown
References: 267651

Otherwise 'modprobe -r' on a module having a dependency on bridge will
implicitly unload bridge, bringing down all connectivity that was using
bridges.

---
 net/bridge/br_if.c |    9 +++++++++
 1 file changed, 9 insertions(+)

Index: linux-3.0-tmp-jikos/net/bridge/br_if.c
===================================================================
--- linux-3.0-tmp-jikos.orig/net/bridge/br_if.c
+++ linux-3.0-tmp-jikos/net/bridge/br_if.c
@@ -248,6 +248,11 @@ int br_del_bridge(struct net *net, const
 	struct net_device *dev;
 	int ret = 0;
 
+	if (!try_module_get(THIS_MODULE)) {
+		free_netdev(dev);
+		return -ENOENT;
+	}
+
 	rtnl_lock();
 	dev = __dev_get_by_name(net, name);
 	if (dev == NULL)
@@ -267,6 +272,8 @@ int br_del_bridge(struct net *net, const
 		del_br(netdev_priv(dev), NULL);
 
 	rtnl_unlock();
+	if (ret == 0)
+		module_put(THIS_MODULE);
 	return ret;
 }
 
