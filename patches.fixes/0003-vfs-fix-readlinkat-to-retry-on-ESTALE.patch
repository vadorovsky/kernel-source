From: Jeff Layton <jlayton@redhat.com>
Date: Tue, 11 Dec 2012 12:10:06 -0500
Subject: [PATCH 03/28] vfs: fix readlinkat to retry on ESTALE
Git-commit: 7955119e02d9fdf78a39fba8073f19ca6152613e
Patch-mainline: v3.8
References: bnc#876463

Signed-off-by: Jeff Layton <jlayton@redhat.com>
Signed-off-by: Al Viro <viro@zeniv.linux.org.uk>
Acked-by: NeilBrown <neilb@suse.de>

---
 fs/stat.c |    8 +++++++-
 1 file changed, 7 insertions(+), 1 deletion(-)

--- linux-3.0-SLE11-SP3.orig/fs/stat.c
+++ linux-3.0-SLE11-SP3/fs/stat.c
@@ -301,11 +301,13 @@ SYSCALL_DEFINE4(readlinkat, int, dfd, co
 	struct path path;
 	int error;
 	int empty = 0;
+	unsigned int lookup_flags = LOOKUP_EMPTY;
 
 	if (bufsiz <= 0)
 		return -EINVAL;
 
-	error = user_path_at_empty(dfd, pathname, LOOKUP_EMPTY, &path, &empty);
+retry:
+	error = user_path_at_empty(dfd, pathname, lookup_flags, &path, &empty);
 	if (!error) {
 		struct inode *inode = path.dentry->d_inode;
 
@@ -319,6 +321,10 @@ SYSCALL_DEFINE4(readlinkat, int, dfd, co
 			}
 		}
 		path_put(&path);
+		if (retry_estale(error, lookup_flags)) {
+			lookup_flags |= LOOKUP_REVAL;
+			goto retry;
+		}
 	}
 	return error;
 }
