From: Hannes Reinecke <hare@suse.de>
Date: Fri, 7 Jun 2013 08:25:14 +0200
Subject: advansys: Remove 'last_reset' references
References: bnc#798050
Patch-Mainline: v3.14

Serves no purpose whatsoever.

Signed-off-by: Hannes Reinecke <hare@suse.de>
---
 drivers/scsi/advansys.c | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/scsi/advansys.c b/drivers/scsi/advansys.c
index b756041..7a7400c 100644
--- a/drivers/scsi/advansys.c
+++ b/drivers/scsi/advansys.c
@@ -2529,8 +2529,8 @@ static void asc_prt_scsi_host(struct Scsi_Host *s)
 	struct asc_board *boardp = shost_priv(s);
 
 	printk("Scsi_Host at addr 0x%p, device %s\n", s, dev_name(boardp->dev));
-	printk(" host_busy %u, host_no %d, last_reset %d,\n",
-	       s->host_busy, s->host_no, (unsigned)s->last_reset);
+	printk(" host_busy %u, host_no %d,\n",
+	       s->host_busy, s->host_no);
 
 	printk(" base 0x%lx, io_port 0x%lx, irq %d,\n",
 	       (ulong)s->base, (ulong)s->io_port, boardp->irq);
@@ -3559,8 +3559,8 @@ static int asc_prt_driver_conf(struct Scsi_Host *shost, char *cp, int cplen)
 	ASC_PRT_NEXT();
 
 	len = asc_prt_line(cp, leftlen,
-			   " host_busy %u, last_reset %u, max_id %u, max_lun %u, max_channel %u\n",
-			   shost->host_busy, shost->last_reset, shost->max_id,
+			   " host_busy %u, max_id %u, max_lun %u, max_channel %u\n",
+			   shost->host_busy, shost->max_id,
 			   shost->max_lun, shost->max_channel);
 	ASC_PRT_NEXT();
 
-- 
1.7.12.4

