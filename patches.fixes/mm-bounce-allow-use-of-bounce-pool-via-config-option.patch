From: Chris Metcalf <cmetcalf@tilera.com>
Date: Sat, 16 Jun 2012 16:41:05 -0400
Subject: [PATCH] bounce: allow use of bounce pool via config option

References: Bounce memory pool initialisation (bnc#836347)
Patch-mainline: Yes (v3.6)
Git-commit: f1006257893917dfb1e0d74cb47b18c0e2908693

The tilegx USB OHCI support needs the bounce pool since we're not
using the IOMMU to handle 32-bit addresses.

Signed-off-by: Chris Metcalf <cmetcalf@tilera.com>
Signed-off-by: Mel Gorman <mgorman@suse.de>
---
 arch/tile/Kconfig | 6 ++++++
 mm/bounce.c       | 8 +++++---
 2 files changed, 11 insertions(+), 3 deletions(-)

diff --git a/arch/tile/Kconfig b/arch/tile/Kconfig
index e4e08b80..33d529a 100644
--- a/arch/tile/Kconfig
+++ b/arch/tile/Kconfig
@@ -341,6 +341,12 @@ config NO_IOPORT
 
 source "drivers/pci/Kconfig"
 
+# USB OHCI needs the bounce pool since tilegx will often have more
+# than 4GB of memory, but we don't currently use the IOTLB to present
+# a 32-bit address to OHCI.  So we need to use a bounce pool instead.
+config NEED_BOUNCE_POOL
+	def_bool USB_OHCI_HCD
+
 config HOTPLUG
 	bool "Support for hot-pluggable devices"
 	---help---
diff --git a/mm/bounce.c b/mm/bounce.c
index 192f29d..a780971 100644
--- a/mm/bounce.c
+++ b/mm/bounce.c
@@ -24,23 +24,25 @@
 
 static mempool_t *page_pool, *isa_page_pool;
 
-#ifdef CONFIG_HIGHMEM
+#if defined(CONFIG_HIGHMEM) || defined(CONFIG_NEED_BOUNCE_POOL)
 static __init int init_emergency_pool(void)
 {
-#ifndef CONFIG_MEMORY_HOTPLUG
+#if defined(CONFIG_HIGHMEM) && !defined(CONFIG_MEMORY_HOTPLUG)
 	if (max_pfn <= max_low_pfn)
 		return 0;
 #endif
 
 	page_pool = mempool_create_page_pool(POOL_SIZE, 0);
 	BUG_ON(!page_pool);
-	printk("highmem bounce pool size: %d pages\n", POOL_SIZE);
+	printk("bounce pool size: %d pages\n", POOL_SIZE);
 
 	return 0;
 }
 
 __initcall(init_emergency_pool);
+#endif
 
+#ifdef CONFIG_HIGHMEM
 /*
  * highmem version, map in to vec
  */
