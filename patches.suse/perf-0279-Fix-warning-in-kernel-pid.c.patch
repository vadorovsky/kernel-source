From: Robert Richter <robert.richter@amd.com>
Date: Wed, 9 Feb 2011 15:25:09 +0100
Subject: [PATCH 279/279] Fix warning in kernel/pid.c
Patch-mainline: no
References: FATE#311392, BNC#685313
Signed-off-by: Tony Jones <tonyj@suse.de>

Fix the following warning:

 kernel/pid.c:515: warning: comparison of distinct pointer types lacks a cast

Signed-off-by: Robert Richter <robert.richter@amd.com>
---
 kernel/pid.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/kernel/pid.c b/kernel/pid.c
index 789c63b..231b59f 100644
--- a/kernel/pid.c
+++ b/kernel/pid.c
@@ -516,9 +516,9 @@ void __init pidmap_init(void)
 {
 	/* bump default and minimum pid_max based on number of cpus */
 	pid_max = min(pid_max_max, max(pid_max,
-				PIDS_PER_CPU_DEFAULT * num_possible_cpus()));
+				(int)(PIDS_PER_CPU_DEFAULT * num_possible_cpus())));
 	pid_max_min = max(pid_max_min,
-				PIDS_PER_CPU_MIN * num_possible_cpus());
+			  (int)(PIDS_PER_CPU_MIN * num_possible_cpus()));
 	pr_info("pid_max: default: %u minimum: %u\n", pid_max, pid_max_min);
 
 	init_pid_ns.pidmap[0].page = kzalloc(PAGE_SIZE, GFP_KERNEL);
-- 
1.7.3.4

