From foo@baz Fri Sep 16 16:42:17 CEST 2011
Date: Fri, 16 Sep 2011 16:42:17 +0200
To: Greg KH <gregkh@suse.de>
From: Greg Kroah-Hartman <gregkh@suse.de>
Subject: kabi: add padding to USB host structures for potential security fixes
References: FATE#312013
Patch-mainline: never


Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>

---
 include/linux/usb/hcd.h |    6 ++++++
 1 file changed, 6 insertions(+)

--- a/include/linux/usb/hcd.h
+++ b/include/linux/usb/hcd.h
@@ -174,6 +174,9 @@ struct usb_hcd {
 	 * (ohci 32, uhci 1024, ehci 256/512/1024).
 	 */
 
+	/* To by used for SLE security fixes if we run into a KABI problem */
+	void *suse_kabi_padding;
+
 	/* The HC driver's private data is stored at the end of
 	 * this structure.
 	 */
@@ -343,6 +346,9 @@ struct hc_driver {
 		 * address is set
 		 */
 	int	(*update_device)(struct usb_hcd *, struct usb_device *);
+
+	/* To by used for SLE security fixes if we run into a KABI problem */
+	void *suse_kabi_padding;
 };
 
 extern int usb_hcd_link_urb_to_ep(struct usb_hcd *hcd, struct urb *urb);
