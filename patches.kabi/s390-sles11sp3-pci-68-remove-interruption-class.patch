From: Gerald Schaefer <gerald.schaefer@de.ibm.com>
Subject: s390/pci: remove PCI/MSI interruption class
Patch-mainline: Never, kabi
References: FATE#83037, LTC#94737

Remove the PCI/MSI interruption class, because this would have
added massive kabi changes.

Signed-off-by: Gerald Schaefer <gerald.schaefer@de.ibm.com>
Acked-by: Michal Marek <mmarek@suse.cz>
---
 arch/s390/include/asm/irq.h |    2 --
 arch/s390/pci/pci.c         |    2 --
 2 files changed, 4 deletions(-)

--- a/arch/s390/include/asm/irq.h
+++ b/arch/s390/include/asm/irq.h
@@ -30,8 +30,6 @@ enum interruption_class {
 	IOINT_CTC,
 	IOINT_APB,
 	IOINT_ADM,
-	IOINT_PCI,
-	IOINT_MSI,
 	NMI_NMI,
 	IOINT_VIR,
 	NR_IRQS,
--- a/arch/s390/pci/pci.c
+++ b/arch/s390/pci/pci.c
@@ -393,7 +393,6 @@ static void zpci_irq_handler(struct airq
 	struct zdev_irq_map *imap;
 	int irqs_on = 0;
 
-	kstat_cpu(smp_processor_id()).irqs[IOINT_PCI]++;
 	for (si = 0;;) {
 		/* Scan adapter summary indicator bit vector */
 		si = airq_iv_scan(zpci_aisb_iv, si, airq_iv_end(zpci_aisb_iv));
@@ -413,7 +412,6 @@ static void zpci_irq_handler(struct airq
 			ai = airq_iv_scan(imap->aibv, ai, imap->msi_vecs);
 			if (ai == -1UL)
 				break;
-			kstat_cpu(smp_processor_id()).irqs[IOINT_MSI]++;
 			airq_iv_lock(imap->aibv, ai);
 			if (imap->cb[ai].handler)
 				imap->cb[ai].handler(ai, imap->cb[ai].data);
