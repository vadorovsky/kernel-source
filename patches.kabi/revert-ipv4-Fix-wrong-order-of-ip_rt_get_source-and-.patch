From: Jiri Slaby <jslaby@suse.cz>
Date: Thu, 1 Mar 2012 15:41:57 +0100
Subject: Revert "ipv4: Fix wrong order of ip_rt_get_source() and update
 iph->daddr."
Patch-mainline: never

This reverts commit ab2fd30a38e23f0b6f2e331889b31f4f6fb2378a, upstream
5dc7883f2a7c25f8df40d7479687153558cd531b.

This is a fix for commit which will be reverted in the next patch.

Signed-off-by: Jiri Slaby <jslaby@suse.cz>
---
 net/ipv4/ip_options.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/net/ipv4/ip_options.c b/net/ipv4/ip_options.c
index 42dd1a9..1e60f76 100644
--- a/net/ipv4/ip_options.c
+++ b/net/ipv4/ip_options.c
@@ -573,8 +573,8 @@ void ip_forward_options(struct sk_buff *skb)
 		}
 		if (srrptr + 3 <= srrspace) {
 			opt->is_changed = 1;
-			ip_hdr(skb)->daddr = opt->nexthop;
 			ip_rt_get_source(&optptr[srrptr-1], skb, rt);
+			ip_hdr(skb)->daddr = opt->nexthop;
 			optptr[2] = srrptr+4;
 		} else if (net_ratelimit())
 			printk(KERN_CRIT "ip_forward(): Argh! Destination lost!\n");
-- 
1.7.9.2

