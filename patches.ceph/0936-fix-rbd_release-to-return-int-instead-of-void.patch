From b0343d6169f7ce71b7839aa5cbd8754abedc2cb3 Mon Sep 17 00:00:00 2001
From: Danny Al-Gaaf <danny.al-gaaf@suse.de>
Date: Mon, 3 Jun 2013 18:16:04 +0200
Subject: [PATCH 936/938] fix rbd_release() to return int instead of void
Patch-mainline: 3.10-rc2
References: fate#312983

Signed-off-by: Danny Al-Gaaf <danny.al-gaaf@bisect.de>
Acked-by: Danny Al-Gaaf <dalgaaf@suse.de>

---
 drivers/block/rbd.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/drivers/block/rbd.c b/drivers/block/rbd.c
index 4e68d16..e3d3c35 100644
--- a/drivers/block/rbd.c
+++ b/drivers/block/rbd.c
@@ -498,7 +498,7 @@ static int rbd_open(struct block_device *bdev, fmode_t mode)
 	return 0;
 }
 
-static void rbd_release(struct gendisk *disk, fmode_t mode)
+static int rbd_release(struct gendisk *disk, fmode_t mode)
 {
 	struct rbd_device *rbd_dev = disk->private_data;
 	unsigned long open_count_before;
@@ -511,6 +511,7 @@ static void rbd_release(struct gendisk *disk, fmode_t mode)
 	mutex_lock_nested(&ctl_mutex, SINGLE_DEPTH_NESTING);
 	put_device(&rbd_dev->dev);
 	mutex_unlock(&ctl_mutex);
+	return 0;
 }
 
 static const struct block_device_operations rbd_bd_ops = {
-- 
1.8.3

