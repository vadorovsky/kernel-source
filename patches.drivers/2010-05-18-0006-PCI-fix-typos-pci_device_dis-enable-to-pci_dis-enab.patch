From: Roman Fietze <roman.fietze@telemotive.de>
Subject: PCI: fix typos pci_device_dis/enable to pci_dis/enable_device in comments
References: fate#311968
Patch-Mainline: yes
Git-commit: ee6583f6e8f8dad4a53985dbabcd7c242d66a6b6


Signed-off-by: Thomas Renninger <trenn@suse.de>

This fixes all occurrences of pci_enable_device and pci_disable_device
in all comments. There are no code changes involved.

Signed-off-by: Roman Fietze <roman.fietze@telemotive.de>
Signed-off-by: Jesse Barnes <jbarnes@virtuousgeek.org>

---
 drivers/edac/amd76x_edac.c     |    2 +-
 drivers/edac/i82443bxgx_edac.c |    2 +-
 drivers/edac/r82600_edac.c     |    2 +-
 drivers/pci/pci.c              |    2 +-
 4 files changed, 4 insertions(+), 4 deletions(-)

Index: linux-2.6.32-SLE11-SP1/drivers/edac/amd76x_edac.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/edac/amd76x_edac.c
+++ linux-2.6.32-SLE11-SP1/drivers/edac/amd76x_edac.c
@@ -294,7 +294,7 @@ static int __devinit amd76x_init_one(str
 {
 	debugf0("%s()\n", __func__);
 
-	/* don't need to call pci_device_enable() */
+	/* don't need to call pci_enable_device() */
 	return amd76x_probe1(pdev, ent->driver_data);
 }
 
Index: linux-2.6.32-SLE11-SP1/drivers/edac/i82443bxgx_edac.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/edac/i82443bxgx_edac.c
+++ linux-2.6.32-SLE11-SP1/drivers/edac/i82443bxgx_edac.c
@@ -354,7 +354,7 @@ static int __devinit i82443bxgx_edacmc_i
 
 	debugf0("MC: " __FILE__ ": %s()\n", __func__);
 
-	/* don't need to call pci_device_enable() */
+	/* don't need to call pci_enable_device() */
 	rc = i82443bxgx_edacmc_probe1(pdev, ent->driver_data);
 
 	if (mci_pdev == NULL)
Index: linux-2.6.32-SLE11-SP1/drivers/edac/r82600_edac.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/edac/r82600_edac.c
+++ linux-2.6.32-SLE11-SP1/drivers/edac/r82600_edac.c
@@ -354,7 +354,7 @@ static int __devinit r82600_init_one(str
 {
 	debugf0("%s()\n", __func__);
 
-	/* don't need to call pci_device_enable() */
+	/* don't need to call pci_enable_device() */
 	return r82600_probe1(pdev, ent->driver_data);
 }
 
Index: linux-2.6.32-SLE11-SP1/drivers/pci/pci.c
===================================================================
--- linux-2.6.32-SLE11-SP1.orig/drivers/pci/pci.c
+++ linux-2.6.32-SLE11-SP1/drivers/pci/pci.c
@@ -1119,7 +1119,7 @@ void pci_disable_enabled_device(struct p
  * anymore.  This only involves disabling PCI bus-mastering, if active.
  *
  * Note we don't actually disable the device until all callers of
- * pci_device_enable() have called pci_device_disable().
+ * pci_enable_device() have called pci_disable_device().
  */
 void
 pci_disable_device(struct pci_dev *dev)
