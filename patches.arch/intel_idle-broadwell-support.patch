From: Len Brown <len.brown@intel.com>
Date: Tue, 4 Feb 2014 23:56:40 -0500
Subject: intel_idle: Broadwell support
Git-commit: a138b56800f4b83a7af69a9958d04f0f124eb37b
Patch-mainline: v3.17-rc1
References: fate#317424

Broadwell (BDW) is similar to Haswell (HSW), the preceding processor generation.

Currently, the only difference in their C-state tables is that PC3 max exit latency
is 33usec on HSW and 40usec on BDW.

Signed-off-by: Len Brown <len.brown@intel.com>
Acked-by: Borislav Petkov <bp@suse.de>
---
 drivers/idle/intel_idle.c | 73 +++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 73 insertions(+)

Index: linux-3.0-SLE11-SP4/drivers/idle/intel_idle.c
===================================================================
--- linux-3.0-SLE11-SP4.orig/drivers/idle/intel_idle.c	2015-05-21 17:49:09.134676703 +0200
+++ linux-3.0-SLE11-SP4/drivers/idle/intel_idle.c	2015-05-21 17:50:53.524541786 +0200
@@ -278,6 +278,73 @@
 		.enter = &intel_idle },
 };
 
+static struct cpuidle_state bdw_cstates[MWAIT_MAX_NUM_CSTATES] = {
+	{
+		.name = "C1-BDW",
+		.desc = "MWAIT 0x00",
+		.driver_data = (void *) 0x00,
+		.flags = CPUIDLE_FLAG_TIME_VALID,
+		.exit_latency = 2,
+		.target_residency = 2,
+		.enter = &intel_idle },
+	{
+		.name = "C1E-BDW",
+		.desc = "MWAIT 0x01",
+		.driver_data = (void *) 0x01,
+		.flags = CPUIDLE_FLAG_TIME_VALID,
+		.exit_latency = 10,
+		.target_residency = 20,
+		.enter = &intel_idle },
+	{
+		.name = "C3-BDW",
+		.desc = "MWAIT 0x10",
+		.driver_data = (void *) 0x10,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 40,
+		.target_residency = 100,
+		.enter = &intel_idle },
+	{
+		.name = "C6-BDW",
+		.desc = "MWAIT 0x20",
+		.driver_data = (void *) 0x20,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 133,
+		.target_residency = 400,
+		.enter = &intel_idle },
+	{
+		.name = "C7s-BDW",
+		.desc = "MWAIT 0x32",
+		.driver_data = (void *) 0x32,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 166,
+		.target_residency = 500,
+		.enter = &intel_idle },
+	{
+		.name = "C8-BDW",
+		.desc = "MWAIT 0x40",
+		.driver_data = (void *) 0x40,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 300,
+		.target_residency = 900,
+		.enter = &intel_idle },
+	{
+		.name = "C9-BDW",
+		.desc = "MWAIT 0x50",
+		.driver_data = (void *) 0x50,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 600,
+		.target_residency = 1800,
+		.enter = &intel_idle },
+	{
+		.name = "C10-BDW",
+		.desc = "MWAIT 0x60",
+		.driver_data = (void *) 0x60,
+		.flags = CPUIDLE_FLAG_TIME_VALID | CPUIDLE_FLAG_TLB_FLUSHED,
+		.exit_latency = 2600,
+		.target_residency = 7700,
+		.enter = &intel_idle },
+};
+
 /**
  * intel_idle
  * @dev: cpuidle_device
@@ -443,6 +510,13 @@
 		cpuidle_state_table = hsw_cstates;
 		break;
 
+	case 0x3d:	/* BDW */
+	case 0x47:	/* BDW */
+	case 0x4f:	/* BDW */
+	case 0x56:	/* BDW */
+		cpuidle_state_table = bdw_cstates;
+		break;
+
 	default:
 		pr_debug(PREFIX "does not run on family %d model %d\n",
 			boot_cpu_data.x86, boot_cpu_data.x86_model);
