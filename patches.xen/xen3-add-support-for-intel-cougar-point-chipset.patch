From: seth.heasley@intel.com
Subject: Intel Cougar Point PCH Support
References: FATE#308854 bnc#565845
Patch-mainline: submitted

Intel Cougar Point DeviceID driver patch (ata_piix, ahci, i2c, irq, hd audio,
tco watchdog)

This patch adds Cougar Point DeviceIDs to kernel drivers.  It is targeted at
getting support into SLES11 SP1.  It was built against the SLES11 SP1 Beta1
source.

Acked-by: Jiri Kosina <jkosina@suse.cz>

Automatically created from "patches.drivers/add-support-for-intel-cougar-point-chipset.patch" by xen-port-patches.py

--- sle11sp1-2010-03-29.orig/arch/x86/pci/irq-xen.c	2009-11-06 10:52:09.000000000 +0100
+++ sle11sp1-2010-03-29/arch/x86/pci/irq-xen.c	2010-03-26 17:20:35.000000000 +0100
@@ -610,6 +610,13 @@ static __init int intel_router_probe(str
 		return 1;
 	}
 
+	if ((device >= PCI_DEVICE_ID_INTEL_CPT_LPC_MIN) && 
+		(device <= PCI_DEVICE_ID_INTEL_CPT_LPC_MAX)) {
+		r->name = "PIIX/ICH";
+		r->get = pirq_piix_get;
+		r->set = pirq_piix_set;
+		return 1;
+	}
 	return 0;
 }
 
